# ===================================
# LiveKit Voice Agent - Environment Configuration
# ===================================

# ===================================
# LiveKit Configuration (Required)
# ===================================
# Get these from: https://cloud.livekit.io
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=APIxxxxxxxxxx
LIVEKIT_API_SECRET=your_secret_key

# LiveKit SIP Domain (for phone calls)
# Default: sip.livekit.cloud
# Get from LiveKit dashboard SIP settings
LIVEKIT_SIP_DOMAIN=sip.livekit.cloud

# ===================================
# Twilio Configuration (Required)
# ===================================
# Get these from: https://console.twilio.com
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# ===================================
# OpenAI Configuration (Required)
# ===================================
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ===================================
# Agent Configuration (Optional)
# ===================================
AGENT_NAME=AI Assistant

# Voice Model - ULTRA-LOW LATENCY OPTIMIZED
# alloy = FASTEST (recommended for lowest latency)
# echo, nova = Fast alternatives
# fable, onyx, shimmer = Slower but more expressive
VOICE_MODEL=alloy

# System Prompt - ULTRA-SHORT for MAXIMUM SPEED
# Every word adds latency - keep it minimal!
SYSTEM_PROMPT=Friendly AI assistant. Be brief. 1 sentence answers. Speak fast.

# ===================================
# Server Configuration (Optional)
# ===================================
WEBHOOK_PORT=8000
WEBHOOK_HOST=0.0.0.0

# LOG_LEVEL - Set to WARNING for production (lower overhead)
# Options: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=WARNING

# ===================================
# ULTRA-LOW LATENCY Performance Settings
# ===================================
# Optimized for <500ms response time + 100+ concurrent users

# Concurrency (how many simultaneous calls)
MAX_CONCURRENT_CALLS=200

# Webhook server worker threads (for concurrent call handling)
MAX_WORKERS=4

# ===================================
# Advanced: Fine-Tuning (Optional)
# ===================================
# These settings are hard-coded in agent.py for optimal performance
# To modify, edit agent.py directly:
#
# VAD Settings (Voice Activity Detection):
#   threshold=0.5              # AGGRESSIVE (faster detection)
#   silence_duration_ms=200    # ULTRA-FAST (quick response)
#   prefix_padding_ms=50       # MINIMAL (low overhead)
#
# LLM Settings:
#   temperature=0.5            # OPTIMIZED (faster, deterministic)
#   modalities=["audio"]       # Audio-only (no text processing)
#
# Trade-offs:
#   - Lower silence_duration_ms = Faster response but may interrupt user
#   - Lower threshold = More sensitive but may trigger on noise
#   - Lower temperature = Faster but less creative responses
#
# Current settings provide ABSOLUTE LOWEST LATENCY while maintaining usability.

